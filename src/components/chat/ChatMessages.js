import { Container } from "react-bootstrap";

const ChatMessages = () => {

    return (
        <Container className="bg-light rounded flex-fill">

        </Container>
    );
}

export default ChatMessages;
